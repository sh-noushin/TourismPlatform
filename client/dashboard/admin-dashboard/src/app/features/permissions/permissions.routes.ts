import { Routes } from '@angular/router';
import { PermissionsPageComponent } from './pages/list/permissions-page.component';

export const routes: Routes = [
  { path: '', component: PermissionsPageComponent }
];
